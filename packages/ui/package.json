{
  "name": "@klarify/ui",
  "version": "0.0.0",
  "private": true,
  "description": "Klarify UI — React Native Web components (shared ~60% between web and mobile)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./components": "./src/components/index.ts",
    "./tokens": "./src/tokens/index.ts",
    "./platform": "./src/platform/index.ts"
  },
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@klarify/config": "workspace:*",
    "@types/react": "^18.3.3",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "@klarify/core": "workspace:*",
    "react-native": "0.74.5"
  }
}
